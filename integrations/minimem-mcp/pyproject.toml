[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "minimem-mcp-server"
version = "0.1.0"
description = "MCP bridge server for MiniMem HTTP API"
requires-python = ">=3.11"
dependencies = [
  "fastmcp>=2.14.0",
]
